"""Per-session xlsx workbook generation (openpyxl) → exports zone.

The "deliver to business users" artifact: one workbook per session written to
``{env}/exports/`` (keys per BACKEND_SCHEMA §1).

Implemented in Phase 2 (see docs/IMPLEMENTATION_PLAN.md).
"""
