# PitWall runtime dependencies (pinned; floor Python 3.12, local+CI run 3.13 — full set verified on 3.13.1, 2026-07-11).
# Provider-neutral: storage is any S3-compatible endpoint (Backblaze B2 today).

# Extraction
httpx==0.28.1

# Transformation + validation
pandas==2.2.3
pandera==0.22.1
pyarrow==18.1.0

# Object storage (S3-compatible via boto3) + warehouse
boto3==1.35.99
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Orchestration
prefect==3.1.15

# Config
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Exports
openpyxl==3.1.5

# Dashboard (Streamlit Community Cloud reads Postgres directly)
streamlit==1.41.1
plotly==5.24.1
